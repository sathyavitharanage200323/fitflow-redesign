# Cloud AI

Server-side AI inference service (e.g., pose estimation fallback, model serving).

## Stack

Python 3.11 + FastAPI + TensorFlow/PyTorch (TBD).

## Run

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```